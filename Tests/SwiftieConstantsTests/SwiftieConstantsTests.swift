import XCTest
@testable import SwiftieConstants

final class SwiftieConstantsTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftieConstants().text, "Hello, World!")
    }
}
